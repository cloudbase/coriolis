---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-licensing-ui

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ docker_images_namespace }}/coriolis-licensing-ui:{{ docker_images_tag }}"
