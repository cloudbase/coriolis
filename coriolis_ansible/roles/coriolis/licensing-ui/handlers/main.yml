---
- name: "build coriolis-licensing-ui"
  include_tasks: "{{ playbook_dir }}/roles/common/docker/tasks/build_image.yml"
  vars:
    image_name: coriolis-licensing-ui

- name: "setup coriolis-licensing-ui"
  include_tasks: "{{ role_path }}/tasks/setup_licensing_ui_container.yml"
  vars:
    restart: yes
