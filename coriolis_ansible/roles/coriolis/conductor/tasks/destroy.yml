---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-conductor

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ coriolis_conductor_docker_image_url }}"

- name: Remove locks directory
  file:
    state: absent
    path: "{{ coriolis_conductor_locks_dir_host }}"
