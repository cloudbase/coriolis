---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-web

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ docker_registry }}/{{ docker_namespace }}/coriolis-web"

- name: "Trigger coriolis-web update"
  command: /bin/true
  notify:
    - "setup coriolis-web"
