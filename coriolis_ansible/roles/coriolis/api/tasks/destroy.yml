---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-api

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ coriolis_api_docker_image_url }}"
