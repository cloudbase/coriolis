---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-compressor

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ coriolis_compressor_docker_image_url }}"
