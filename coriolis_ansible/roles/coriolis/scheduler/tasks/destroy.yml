---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-scheduler

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ docker_registry }}/{{ docker_namespace }}/coriolis-scheduler:{{ docker_images_tag }}"
