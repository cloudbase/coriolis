FROM {{ docker_registry }}/{{ docker_namespace }}/coriolis-common:{{ docker_images_tag }}

# Mapped volume: /etc/coriolis/coriolis.conf
{% if "scheduler" in enable_profiling_for %}
CMD ["/usr/local/bin/fil-profile", "-o", "{{ coriolis_profiling_dir_container }}", "run", "/usr/local/bin/coriolis-scheduler", "--config-file={{ coriolis_config_dir }}/coriolis.conf", "--config-file={{ coriolis_config_dir }}/coriolis-scheduler-logging.conf"]
{% else %}
CMD ["/bin/bash", "-c", "coriolis-scheduler", "--config-file={{ coriolis_config_dir }}/coriolis.conf"]
{% endif %}
