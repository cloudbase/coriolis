---
- name: Remove Docker container
  docker_container:
    state: absent
    name: coriolis-transfer-cron

- name: Remove Docker image
  docker_image:
    state: absent
    force_absent: yes
    name: "{{ coriolis_transfer_cron_docker_image_url }}"
