FROM {{ docker_registry }}/coriolis-common:{{ docker_images_tag }}
# Mapped volume: /etc/coriolis/coriolis.conf
ENTRYPOINT ["/bin/bash", "-c", "coriolis-replica-cron", "--config-file=/etc/coriolis/coriolis.conf"]
